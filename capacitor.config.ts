import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.usac.statwo',
  appName: 'statwo-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
